# P1 Implementation Handoff: HDF5 Canonical Benchmark v4 Small

- Date: 2026-07-24
- Program: `SEGMENTATION_MODEL_CONTINUATION`
- Phase status: `implementation_complete_awaiting_owner_gate`
- Branch: `refactor/sami-groundsegdesc`
- HEAD: `58813468d2cf3be715e399f5904a8d9f7f5c880d`
- Commit/push: not performed
- Builder/independent validator: not run by Codex
- P2 authorization: false until live P1 acceptance

## Outcome

P1 now has one incompatible, self-contained Benchmark v4 implementation. It reads the five ready
HDF5 sources, emits no Sen12 row, preserves every frozen native split, keeps Landslide4Sense
train-only, computes normalization from canonical-train valid pixels, and byte-copies every selected
image/mask HDF5 below a source-organized Benchmark `assets/` tree.

The implementation explicitly binds:

- the complete frozen index set for every source;
- five ready sources and one not-ready source;
- `311` LandslideBench_agent location-level conflicts from the P0R inventory;
- all P0R risk strings, including 114 all-zero slope observations and invalid InSAR pixels;
- null split assurance/evaluation eligibility for not-ready Sen12;
- explicit zero populations for `verified_group_isolated` and `strict`.
- source and Benchmark copy paths, identical hashes, byte sizes, and a complete materialization
  ledger;
- a 19-key channel catalog that maps source-native indices to explicit physical channel identities
  without reordering stored tensors.

This is not P1 acceptance. No live Benchmark v4 artifact or independent validation report exists.

## Implementation paths

Configuration and contracts:

- `configs/benchmark_v4_small.yaml`
- `src/sami_gsd/contracts/benchmark_v4.py`
- `src/sami_gsd/contracts/benchmark_v4_config.py`
- `schemas/hdf5_source_record_v1.schema.json`
- `schemas/canonical_parent_v4.schema.json`
- `schemas/benchmark_manifest_v4.schema.json`
- `schemas/benchmark_statistics_v4.schema.json`
- `schemas/benchmark_validation_report_v4.schema.json`
- `schemas/channel_catalog_v1.schema.json`
- `schemas/materialized_hdf5_asset_v1.schema.json`

Runtime:

- `src/sami_gsd/data/hdf5_sources_v4.py`
- `src/sami_gsd/data/benchmark_v4.py`
- `src/sami_gsd/data/benchmark_v4_validation.py`
- `src/sami_gsd/cli.py`
- `src/sami_gsd/__init__.py`
- `src/sami_gsd/contracts/__init__.py`
- `src/sami_gsd/data/__init__.py`

Focused tests:

- `tests/p1/v4_test_support.py`
- `tests/p1/test_v4_contracts_config.py`
- `tests/p1/test_v4_source_records.py`
- `tests/p1/test_v4_builder_validation.py`
- `tests/p1/test_v4_cli.py`

## Codex evidence

| Check | Exit | Result |
| --- | ---: | --- |
| qwen3vl dependency import | 0 | `h5py 3.16.0`, `numpy 2.1.2`, `yaml 6.0.2`, `jsonschema 4.26.0` |
| production config expansion | 0 | six source configs, five ready sources, 29 bindings / 19 channel keys |
| P1 CLI help/import | 0 | only explicit `benchmark build` and `benchmark validate` routes |
| focused `unittest` | 0 | 19 tests passed |
| JSON Schema meta-validation | 0 | seven P1 schemas passed |
| selected HDF5 stat preflight | 0 | 107,290 files; 35,698,325,607 bytes |
| synthetic materialization replay | 0 | independent byte copies, ledger projection, and hash equality passed |
| live first-pair read-only source replay | 0 | one image/mask pair reopened for each of the five ready sources |
| source/parent schema validation | 0 | five replayed source records and canonical parents passed |
| changed Python AST | 0 | 14 modified/new Python files parsed |
| JSON/YAML/Markdown static gate | 0 | 7 JSON, 4 YAML, and 13 Markdown files passed |
| active P1 removed-symbol scan | 0 | no old dirty-P3 symbol in the replacement |
| `git diff --check` | 0 | passed |
| owner target/report absence | 0 | both fail-on-existing paths are absent |

The initial combined dependency probe exited 1 because `pytest` is not installed in `qwen3vl`;
the suite uses standard-library `unittest`. An early focused run exposed a misplaced contract
validation block and a later run exposed a risk-string drift; both were corrected. A read-only
review also found and corrected an independent-statistics aggregate mismatch that would otherwise
have made every owner validator report fail.

One intentionally broad old-symbol scan exited 1 because it included the committed P2 Qwen
replacement-owned paths. Those files must remain until P2 acceptance. The corrected scan covers
only active P1 replacement paths and passed.

No full builder, independent validator, GPU/CUDA program, micro-overfit, training, evaluation,
formal run, commit, or push was executed. `../datasets` was not modified and `../benchmark`
remained empty at handoff preparation. The selected index projection contains exactly 107,290 HDF5
files and 35,698,325,607 bytes (about 33.25 GiB) by read-only stat replay; the builder repeats this
preflight before staging.

## Owner commands

Run from the repository root:

```bash
cd /home/yukun80/codes/paper7_VLM

PYTHONPATH=src:. /home/yukun80/miniconda3/envs/qwen3vl/bin/python -m unittest -v \
  tests.p1.test_v4_contracts_config \
  tests.p1.test_v4_source_records \
  tests.p1.test_v4_builder_validation \
  tests.p1.test_v4_cli
```

Require a new output path, then build:

```bash
test ! -e /home/yukun80/codes/benchmark/sami_landslide_hdf5_v4/small

PYTHONPATH=src:. /home/yukun80/miniconda3/envs/qwen3vl/bin/python -m sami_gsd.cli \
  benchmark build \
  --config /home/yukun80/codes/paper7_VLM/configs/benchmark_v4_small.yaml \
  --datasets-root /home/yukun80/codes/datasets \
  --benchmark-root /home/yukun80/codes/benchmark
```

Require a new external report path, then validate:

```bash
test ! -e /home/yukun80/codes/paper7_VLM/outputs/sami_gsd/p1/hdf5_v4_small_independent_validation.json

PYTHONPATH=src:. /home/yukun80/miniconda3/envs/qwen3vl/bin/python -m sami_gsd.cli \
  benchmark validate \
  --benchmark /home/yukun80/codes/benchmark/sami_landslide_hdf5_v4/small \
  --datasets-root /home/yukun80/codes/datasets \
  --schemas-root /home/yukun80/codes/paper7_VLM/schemas \
  --output /home/yukun80/codes/paper7_VLM/outputs/sami_gsd/p1/hdf5_v4_small_independent_validation.json
```

Require `errors == []` and the frozen population projection:

```bash
PYTHONPATH=src:. /home/yukun80/miniconda3/envs/qwen3vl/bin/python -c 'import json; from pathlib import Path; p=Path("/home/yukun80/codes/paper7_VLM/outputs/sami_gsd/p1/hdf5_v4_small_independent_validation.json"); d=json.loads(p.read_text(encoding="utf-8"), parse_constant=lambda x: (_ for _ in ()).throw(ValueError(x))); assert d["errors"] == [], d["errors"]; expected={"materialized_asset_count":107290,"source_record_count":53645,"parent_count":53645,"split_counts":{"train":37521,"val":11995,"test":4129},"assurance_counts":{"verified_group_isolated":0,"source_declared_unverified":49846,"train_only":3799},"eligibility_counts":{"strict":0,"exploratory":49846,"train_only":3799}}; assert all(d[k] == v for k,v in expected.items()), {k:d[k] for k in expected}; assert d["materialized_size_bytes"] > 0; print(json.dumps({k:d[k] for k in ("benchmark_manifest_sha256","normalization_binding_sha256","materialized_asset_count","materialized_size_bytes","source_record_count","parent_count","split_counts","assurance_counts","eligibility_counts")}, ensure_ascii=False, sort_keys=True, indent=2))'
```

Require the 19-key channel vocabulary:

```bash
PYTHONPATH=src:. /home/yukun80/miniconda3/envs/qwen3vl/bin/python -c 'import json; from pathlib import Path; p=Path("/home/yukun80/codes/benchmark/sami_landslide_hdf5_v4/small/manifests/channel_catalog.json"); d=json.loads(p.read_text(encoding="utf-8")); keys=[x["channel_key"] for x in d["entries"]]; assert len(keys)==19 and len(keys)==len(set(keys)); print(json.dumps(keys, ensure_ascii=False, indent=2))'
```

Return:

1. every command's exit code;
2. the builder stdout manifest JSON;
3. the complete independent validation report;
4. the printed binding/population summary;
5. the printed channel vocabulary;
6. any traceback and the existing target path if a fail-on-existing check fails.

Do not delete or overwrite an existing target to make the command pass.

## Acceptance and next action

Expected P0R counts are a drift detector, not a replacement for replay. P1 can be accepted only
after the returned report is reopened and confirms:

- `errors == []`;
- all five ready sources and no Sen12 parent;
- 53,645 source records and parents;
- 107,290 independently copied image/mask HDF5 assets and a non-zero exact byte count;
- train/val/test = 37,521 / 11,995 / 4,129;
- assurance = 0 verified / 49,846 source-declared-unverified / 3,799 train-only;
- eligibility = 0 strict / 49,846 exploratory / 3,799 train-only;
- exact source/copy bytes, materialization ledger, HDF5 metadata, channel catalog, source registry,
  manifest, normalization, parent, and split replay;
- 311 LandslideBench_agent location conflicts remain visible in the risk report.

Until then the continuous Goal remains unfinished, P1 remains unaccepted, and P2 code must not be
implemented.
