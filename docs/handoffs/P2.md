# Handoff: P2

## Completion

- Formal phase: `P2`
- Status: `engineering_accepted`
- Completion date: 2026-07-21
- Branch: `refactor/sami-groundsegdesc`
- Completion implementation/report commit: `pending_local_commit`
- Push: not performed
- Formal report: `docs/reports/p2/p2_completion_report.json`
- Official forward report: `docs/reports/p2/profile_s_smoke.json`

P2 is complete as one formal execution unit. Model contracts, implementation, synthetic tests,
official processor probe, real Profile S forward, regression and reporting were internal work
packages, not separate phases or acceptance points.

## Delivered boundary

- Strict `configs/model_sami.yaml` and immutable Pydantic contracts for the official backend,
  processor, S/M pixel budgets, accepted P1 benchmark identity, cache protocol and memory gate.
- `SensorAwareMultiImageAdapter(nn.Module)` with reference-first stable view ordering, explicit
  active subset, no silent truncation, sensor cards, missing/zero-valid/dropout distinction and
  deterministic RGB/valid rendering before forward.
- `QwenBackboneWrapper` using the installed official Qwen3-VL processor, chat template and model
  class. All views for all parents enter exactly one complete official forward.
- Typed task-neutral parent/view states with language-aligned image tokens, per-view DeepStack and
  final spatial grids, processor `grid_thw`, canonical transform chains, valid masks, view identity,
  prompt/model/processor/code fingerprints and active/excluded modality state.
- Optional write-once cache whose key binds model weights, processor, Qwen code revision, exact
  rendered input/card/order/pixel budget/dtype and requested spatial surface. It reopens only the
  greenfield v1 schema and enforces exact metadata/shape/dtype plus numerical thresholds.
- Sole root CLI command `sami-gsd model smoke`, which refuses report overwrite and accepted/nonempty
  cache reuse, replays P1 benchmark binding, runs a bounded official forward and records Profile S.

## Accepted real forward

Input parent: `sen12-chimanimani-1001` from `../benchmark/sami_landslide_v3/small`.

| Evidence | Result |
|---|---|
| Effective order | `s2_optical`, `s1_ascending`, `s1_descending`, `dem` |
| Official calls | one complete native multi-image model forward; second encode is cache-only |
| Reference grid | processor 1x32x32, merged 16x16, 256 language-aligned tokens |
| Support grids | each processor 1x24x24, merged 12x12, 144 language-aligned tokens |
| Spatial state | DeepStack layers 5/11/17 plus final vision grid for every view |
| Cache tensors | 24; metadata/shape/dtype exact |
| Cache numerical result | minimum cosine 0.9999999999999998; maximum absolute difference 0 |
| CUDA peak | allocated 4.1048808097839355 GiB; reserved 4.205078125 GiB |
| Frozen gate | 22 GiB; passed |
| Report | `errors=[]`, `warnings=[]`, `status=engineering_valid` |

The smoke proves the P2 engineering path and memory feasibility. It is not segmentation accuracy,
description factuality, G0 kernel selection, training, or scientific superiority evidence.

## Verification

| Command/check | Exit | Result |
|---|---:|---|
| `python -m py_compile` over new P2 modules and CLI | 0 | passed |
| First focused P2 suite | 0 | 15/15 passed; exposed only a Pillow deprecation, then removed |
| Final focused P2 suite before docs | 0 | 16/16 passed |
| Official processor-only real four-view probe | 0 | four independent grids and 688 merged image tokens |
| First real smoke attempt | 1 | PyTorch 2.8 rejected `torch.device` in peak-stat API before model load; repaired with explicit CUDA index |
| Final official Profile S smoke | 0 | `engineering_valid`, exact cache replay, peak within gate |
| P1 regression suite | 0 | 62/62 passed |
| Combined standard-library regression | 0 | 78/78 P1+P2 tests passed |
| Optional `pytest` invocation | 1 | environment has not installed declared test extra; no collection occurred |
| YAML/JSON/static/import/deletion-manifest/final diff checks | 0 | 39 unique null-gated entries, no deletion/partial/path leak, smoke bytes match, diff clean |

## Deletion state

No legacy code or artifact was physically deleted. The manifest now explicitly includes the old
Qwen vision wrapper, old vision cache and old cache CLI in addition to its existing broad entries.
All 39 entries still have `approved_by=null` and `deleted_commit=null`.

## Remaining boundaries

- Profile M is registered but was not executed or selected by scientific comparison in P2.
- Optional aligned support residual is not enabled; it requires later evidence and an explicit
  accepted decision if native multi-image is insufficient.
- P2 does not implement or choose the grounded segmentation kernel, mask loss, region reader,
  description compiler, trainer or evaluator.
- Accepted P1 benchmark, local model weights, legacy caches/checkpoints and historical outputs
  remain read-only.

## Next accepted task

`P3: G0 segmentation-kernel selection` over the accepted P2 state and fixed pilot. It requires a
new explicit phase confirmation. This handoff does not authorize P3 implementation, formal
training, Full construction, paid API use, expert review or any physical legacy deletion.
